類別 CASC {
    函式 主函式(參數: 字串[]) {
        for i from 0 until 5 {
            println(i)
        }

        for i from 0 to 5 {
            println(i)
        }
    }

    函式 加總(n: 整數, m: 整數 = 2): 整數 {
        return n + m
    }
}