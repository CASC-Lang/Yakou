pkg chaos {
    const a: i32 = 1
    inline static b: i32 = 1

    fn test(kek: str) -> i32 {
        let a = 1
        let b = a + b
        return b
    }

    fn test2(a: i32, b: i32) -> bool =
        1 == 2

    class Person priv(pub firstName: str, pub lastName: str, pub age: i8) {
        name: str = firstName
    }
}
