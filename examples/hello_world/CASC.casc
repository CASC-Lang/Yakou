class CASC {
    abc: int

    priv mut :
        b: int
        ba: long
    intl :
        c: string
    prot mut :
        d: short

    ctor(num: int) {
        abc = num
    }

    fn main(arg: string[]) {
        a := CASC(1)
        b := Student(name="ChAoS".toLowerCase().toUpperCase())
    }
}