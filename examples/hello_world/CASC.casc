priv class CASC {
    abc: int

    priv mut :
        pub b: int
        ba: long
    intl :
        c: string
    prot comp mut :
        d: short

    priv ctor(num: int) {
        abc = num
    }

    comp fn main(arg: string[]) {
        a := CASC(1)
        b := Student(name="ChAoS".toLowerCase().toUpperCase())
        mut c := 1
        c = 2
    }
}