priv class CASC {
    mut abc: int

    priv mut :
        b: i32
        ba: i64
    intl :
        c: str
    prot comp mut :
        d: i16

    priv ctor(num: i32) {
        abc = num
    }

    comp fn main(arg: str[]) {
        a := CASC(1)
        b := Student(name="ChAoS".toLowerCase().toUpperCase())
        mut c := 1
        c = 2
        d := null
    }
}