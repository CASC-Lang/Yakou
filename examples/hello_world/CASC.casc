priv class CASC {
    mut abc: int

    priv mut :
        b: int
        ba: long
    intl :
        c: string
    prot comp mut :
        d: short

    priv ctor(num: int) {
        abc = num
    }

    comp fn main(arg: string[]) {
        a := CASC(1)
        b := Student(name="ChAoS".toLowerCase().toUpperCase())
        mut c := 1
        c = 2
    }

    comp fn test() {
        abc = 1
    }
}