use java::lang::annotation

class CASC {
    comp fn main(arg: str[]) {
        a := annotation::ElementType::METHOD
    }

    fn getNum(b: i32): i32 {
        if b > 12 return b
        else {
            if b > 10 return 10
            else return 0
        }
    }
}
