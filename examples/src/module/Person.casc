mod module

class Person(name: str, age: i32) {
    comp fn hello() {
        println(Integer::valueOf("1"))
    }

    fn prt() {
        print("name: ")
        println(name)
        print("age: ")
        println(age)
    }

    fn toStr(): str {
        return "name: " + name + ", age: "
    }
}