use java::lang::System as sys

class Main {
intl mut:
    a: i32
}

impl Main : Rock {
    comp {
        a := 1B
        b := 1S
        c := 1I
        d := 1L
        e := 1.0F
        f := 1.0D
        g := true
        h := false
        j := 'a'
        k := "\\\t\b\n\r\f\u0001\"\'"
        l := "a"
        m := null
        n := [;]{ 1, 2, 3 }
        o := [i32;]{ 1, 2, 3 }
        p := [i32;10]{}
        q := [[i32;];]{ [;]{ 1 } }
        r := [[i32; 3];3]{}
    }

    fn main(args: [str]) {
        a := new Main()
        a.lol()
        a.print()
        b := null
        sys.out.println(a)
        a.local(new Rock("a"))
    }

    new(k: i32): super(k + "1 + 2") {
        self.a = k
    }

    new(): self(1) {
    }

    ovrd fn lol(self): i32 {
        mut a := "ab"
        super.lol()
        if true {
            return 1
        } else return 1
    }

    fn local(self, atom: Atom) {
        sys.out.println(atom)
    }
}

