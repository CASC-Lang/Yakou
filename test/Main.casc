use java::lang::System as sys

class Main {
intl mut:
    a: i32
}

impl Main : Rock {
    comp {
        fn main(args: str[]) {
            a := new Main()
            a.lol()
            a.print()
            b := null
        }
    }

    new(k: i32): super(k + "1 + 2") {
        self.a = k
    }

    new(): self(1) {}

    ovrd fn lol(): i32 {
        mut a := "ab"
        if true {
            return 1
        } else return 1
    }
}

